import NstpRecordsPage from '@/app/nstp/records/page'

// Reuse the exact same component, but now it renders within the /lgu layout
export default function LguRecordsPage() {
  return <NstpRecordsPage />
}
